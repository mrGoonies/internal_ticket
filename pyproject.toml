[project]
name = "ticket-irritec"
version = "0.1.0"
description = "internal tool for manage IT tickets from final users."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "django-environ>=0.14.0",
]
